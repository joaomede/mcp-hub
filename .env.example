# MCP Hub Environment Configuration

# Port for MCP Hub to listen on
MCP_HUB_PORT=8000

# API Key for authentication
# Leave empty to disable auth in local dev; set a strong key in production
MCP_HUB_API_KEY=

# Log level (debug, info, warning, error)
MCP_HUB_LOG_LEVEL=warning

# Example environment variables for MCP servers
# Add any environment variables your MCP servers might need
# OPENAI_API_KEY=your-openai-key
# ANTHROPIC_API_KEY=your-anthropic-key
# DATABASE_URL=postgresql://user:pass@localhost/db
